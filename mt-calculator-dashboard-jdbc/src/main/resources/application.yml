server:
  port: ${PORT:8080}
spring:
  datasource:
    url: jdbc:sqlserver://31.31.196.202:1433;databaseName=u0932185_DemoBase
    userName: ${sql-user-name}
    password: ${sql-user-pass}
    initialization-mode: never

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

    show-sql: true
    properties:
      hibernate:
      #format_sql: true

app:
  jwt:
    secret: js;retihubserlgbhser/holyiser/blihgser/gblhiserlg

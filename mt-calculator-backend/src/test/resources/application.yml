server:
  port: ${PORT:8080}
spring:
  datasource:
    url: jdbc:h2:mem:test;MODE=MSSQLServer
    initialization-mode: always

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

    show-sql: true
    properties:
      hibernate:
      #format_sql: true

app:
  jwt:
    secret: js;retihubserlgbhser/holyiser/blihgser/gblhiserlg
  session-life-time-sec: 43200
